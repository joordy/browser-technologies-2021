<body class="productPage">
  {{> header}}
  
  <main>
    <picture>
      {{#each product.image.normal}}
        <source media="(min-width:650px)" srcset="{{this}}">
      {{/each}}
      {{#each product.image.small}}
        <img src="{{this}}" alt="">
      {{/each}}
    </picture>
    <h2>{{product.name}}</h2>
    <h3>{{product.price}}</h3>

    <form action="/cart" method="POST">
      <fieldset>
        <input type="hidden" name="orderID" value="{{uniqueID}}">
        <input type="hidden" name="productID" value="{{product.id}}">
        <input type="hidden" name="name" value="{{product.name}}">
        <input type="hidden" name="type" value="{{product.type}}">
        <input type="hidden" name="cat" value="{{product.categorie}}">
        <input type="hidden" name="price" value="{{product.price}}">
        <input type="hidden" name="gender" value="{{product.gender}}">
        <input type="hidden" name="img" value="{{product.image.small}}">

        <div>
          <label for="">Color</label>
            <select name="color" id="color">
              {{#each product.color }}
                <option value="{{this}}">{{this}}</option>
              {{/each}}
            </select>
        </div>
        <div>
          <label for="">Print</label>
          <input type="text" name="print" id="" required>
        </div>
        <div>
          <label for="">Size</label>
          <select name="size" id="size">
              {{#each product.sizes }}
                <option value="{{this}}">{{this}}</option>
              {{/each}}
            </select>
        </div>

        <input type="submit" value="Add to cart">
      </fieldset>
    </form>

    <h4>Product information</h4>
    <section>

    </section>
  </main>
  {{> footer}}

</body>

